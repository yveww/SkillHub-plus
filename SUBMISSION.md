# SkillHub+ 提交材料

> 飞书 AI 绝活大会参赛项目 · yveww · 2026.08

---

## 一、项目介绍

### 它是什么

**SkillHub+** 是一个飞书 Skill 兼容性诊断平台，基于飞书 SkillHub 品牌延展（"+" = 增强版），为已发布的 Skill 提供类 GitHub 的 Issue 反馈、AI 兼容性诊断、版本管理和 Fork 合并能力。

核心是：**用户反馈触发 → AI 多环境兼容性横测 → 输出归因和适配建议**。站在 Skill 端看问题，帮开发者快速定位 Skill 在不同 Agent 环境、不同使用场景下的兼容性问题，给出适配方案。

一句话：**让每个 Skill 都有反馈窗口，每个兼容性问题都能被快速定位和适配。**

核心功能闭环：

```
用户反馈表单 → Issues 表（待诊断）
                    │
               AI 兼容性诊断
               ┌────┼────┐
           Skill Bug  环境兼容  使用问题
               │
         创建修复分支 + 版本记录
               │
         IM 交互卡片通知开发者（含适配建议）
               │
         Forks 表（上游 Diff → 合并）
```

六大功能模块：

| 模块 | 功能 | 飞书原生能力 |
|------|------|-------------|
| 用户反馈 | 表单收集问题，零门槛提交 | 多维表格表单 |
| AI 兼容性诊断 | 多环境横测 → 归因定位 → 适配建议 → 判定 | Python 引擎 |
| Issue 看板 | 看板视图，状态流转可视 | 多维表格看板 |
| 版本管理 | 修复版本记录，关联 Issue | 多维表格记录 |
| Fork 合并 | 上游 Diff 对比，安全合并 | 多维表格记录 |
| IM 通知 | 诊断结果推送，卡片内操作 | Interactive Card 2.0 |

### 为什么做

飞书 Skill 生态正在快速发展，但存在一个核心矛盾：

> **同一个 Skill，不同 Agent 调用，效果天差地别。用户无处反馈，开发者无从知晓。**

具体痛点：

1. **环境差异大** — 同一个 Skill 在 Trae、Doubao、飞书机器人、用户自建 aily 智能体上调用效果完全不同。工具调用格式、上下文长度、系统 Prompt 配置都可能影响结果
2. **无反馈窗口** — 飞书 SkillHub 是市场，不是代码托管平台。用户遇到问题没有类似 GitHub Issue 的反馈渠道
3. **问题沉默默认** — 用户默默忍受或换个环境用，开发者完全不知道自己的 Skill 在哪些环境下跑不通
4. **排查成本高** — 即便收到反馈，开发者要逐一在不同环境下复现、定位是 Skill 问题还是环境问题，效率极低
5. **无版本管理** — Skill 修复和适配后没有版本记录，用户不知道是否已修复，也无法 Fork 自定义

这最终侵蚀 Skill 质量和用户信任。SkillHub+ 就是为了填补这个空白。

### 怎么实现

**一句话概括：飞书负责数据存储和用户触达，AI 负责问题分析和判定，两者通过 lark-cli 桥接。零自建后端。**

```
┌─────────────────────────────────────────────────────────┐
│                    飞书做了什么                            │
│                                                         │
│  数据存储                    用户触达                     │
│  ┌──────────────┐          ┌──────────────────┐         │
│  │  多维表格     │          │  Interactive     │         │
│  │  (Base)     │          │  Card 2.0        │         │
│  │              │          │                  │         │
│  │ Issues 表    │  lark-cli│  分流报告卡片     │         │
│  │ Versions 表  │←───────→│  确认Bug 按钮     │         │
│  │ Forks 表     │   API   │  标记误报 按钮    │         │
│  │              │          │  转人工 按钮      │         │
│  │ 表单收集反馈  │          │                  │         │
│  │ 看板视图      │          │  状态同步回表格   │         │
│  └──────────────┘          └──────────────────┘         │
│         ↑                          ↑                     │
│         │                          │                     │
│  ┌──────┴──────────────────────────┴──────┐              │
│  │            AI 做了什么                   │              │
│  │                                         │              │
│  │  ① 复现测试 → 模拟 Prompt 在 Agent 重跑  │              │
│  │  ② 多Agent对比 → 跨 Agent 差异分析       │              │
│  │  ③ 根因定位 → 读 SKILL.md 定位根因       │              │
│  │  ④ 智能判定 → 输出 Bug/非Bug/待确认       │              │
│  │                                         │              │
│  │  结果写回飞书多维表格                     │              │
│  │  → 触发 IM 卡片推送                      │              │
│  └─────────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────┘
```

#### 飞书做了什么（基础设施层）

飞书提供了三件事，构成了整个产品的骨架：

| 飞书能力 | 在项目中扮演的角色 | 具体 API |
|---------|-------------------|---------|
| **多维表格（Base）** | 唯一的数据存储层，零自建数据库 | `+record-batch-create` 创建记录、`+record-batch-update` 更新记录、`+record-search` 查询 |
| **表单 + 看板视图** | 用户反馈入口 + Issue 状态可视化管理 | `+form-questions-list` 验证表单状态、`+views-create` 创建看板视图 |
| **IM 交互卡片** | 分流结果触达开发者，卡片内直接操作 Issue 状态 | `lark-cli im +messages-send --msg-type interactive` 发送 Interactive Card 2.0 |

简而言之：飞书负责"存数据、收反馈、推通知、给开发者操作入口"。

- **Issues 表**（15 个字段）— Issue ID、标题、Skill 信息、Agent 类型、问题描述、严重程度、状态、AI 判定、AI 置信度、AI 根因分析、复现结果、多 Agent 对比、修复分支
- **Versions 表**（6 个字段）— 版本号、Skill 名称、状态、CHANGELOG、修复 Issue、发布时间
- **Forks 表**（8 个字段）— Fork 名称、原版信息、自定义修改、上游版本、Diff 摘要、合并状态

用户通过飞书表单提交问题 → 数据进入 Issues 表 → AI 引擎处理 → 结果写回 Issues 表的 AI 字段 → IM 卡片推送到开发者飞书 → 开发者在卡片上操作 → 状态同步回 Issues 表。全程数据不离开飞书。

#### AI 做了什么（智能分析层）

AI 引擎（Python 实现）负责最核心的"诊断"工作——从 Skill 端出发，做兼容性横测和归因定位。这是飞书本身做不到的：

| 步骤 | AI 做了什么 | 站在 Skill 端的价值 |
|------|-----------|-------------------|
| ① 问题复现 | 在用户报告的环境中模拟相同 Prompt 重新运行，记录执行日志和异常堆栈 | 确认问题可复现，拿到第一手失败现场 |
| ② 兼容性横测 | 在 Trae / Doubao / 飞书机器人 / 用户 aily 等多个环境下跑同一个 Skill 用例，生成兼容性矩阵 | 知道 Skill 在哪些环境正常、哪些环境异常，快速缩小排查范围 |
| ③ 适配定位 | 分析 Skill 的 SKILL.md 和参考文档，定位需要适配的代码段（如缺少某平台工具调用格式、条件分支未覆盖、上下文处理不足） | 直接告诉开发者改哪一行、怎么改，不用自己排查 |
| ④ 归因判定 | 综合横测结果和定位分析，输出归因分类和适配建议：Skill Bug / 平台兼容 / 配置问题 / 上下文问题 / 使用问题 | 开发者一眼知道这是 Skill 的锅、环境的锅，还是用户的锅 |

**核心逻辑：从 Skill 端看问题，不评估模型好坏，只看 Skill 能不能在各种环境下跑通。**

就像前端测浏览器兼容性——你不会去评价 Chrome 比 Safari 好，你只会说"我的网站在 Safari 上布局乱了，需要适配"。SkillHub+ 做的就是 Skill 的兼容性测试：如果所有环境都失败，那是 Skill 本身的 Bug；如果只有某个环境失败，那是环境兼容问题；如果换个 Prompt 就好了，那是使用问题。

AI 分析完成后，将归因分类、兼容性矩阵、适配建议、修复分支名等写入飞书多维表格的对应字段，触发后续的 IM 卡片推送。开发者拿到的不是"确认 Bug 89%"这么干巴巴的结论，而是"Doubao 平台下工具调用格式不兼容，SKILL.md L32 缺少 Doubao 分支，建议补充 XSL 格式适配"这种直接能用的信息。

#### lark-cli 桥接层

lark-cli 是飞书和 AI 之间的桥梁——AI 引擎通过 lark-cli 读写飞书数据、发送 IM 消息：

```
AI 引擎（Python）
    │
    ├─ subprocess.run(["lark-cli", "base", "+record-batch-create", ...])
    │   → 向 Issues 表插入记录
    │
    ├─ subprocess.run(["lark-cli", "base", "+record-batch-update", ...])
    │   → 更新 Issue 的 AI 判定字段
    │
    └─ subprocess.run(["lark-cli", "im", "+messages-send", "--msg-type", "interactive", ...])
        → 向开发者发送 Interactive Card
```

### 带来了什么变化

**搭建前：**

| 角色 | 痛点 | 状态 |
|------|------|------|
| 用户 | 这个环境用不了，换个环境试试？没人问、没人答 | 问题沉默默认 |
| 开发者 | 不知道 Skill 在哪些环境跑不通，收到反馈也要手动逐一复现排查 | 盲人摸象 |
| 生态 | Skill 发布后就一锤子买卖，没有持续迭代机制 | 不可维护 |

**搭建后：**

| 角色 | 变化 | 效果 |
|------|------|------|
| 用户 | 飞书表单 30 秒提交反馈，可跟踪进度 | 反馈有门 |
| 开发者 | AI 已完成多环境兼容性横测 + 适配定位，直接看归因和修改建议 | 排查效率提升约 10 倍（30 分钟/Issue → 3 分钟/Issue） |
| 生态 | Skill 有了 Issue 看板、版本记录、Fork 合并，像开源项目一样持续迭代 | 质量闭环 |

**实际数据（Demo 运行结果）：**

- 3 个 Issue 提交后，AI 在数秒内完成兼容性诊断
- 诊断结果：1 个 Skill Bug（置信度 89%，Doubao 平台工具调用格式不兼容），1 个使用问题（91%），1 个待确认（52%，疑似 aily 配置覆盖 Skill 指令）
- Skill Bug 自动创建修复分支 `fix/lark-mail-247` 和版本记录 `v1.2.1-fix`，附适配建议
- 开发者在飞书 IM 卡片内点击"确认 Bug"，状态实时同步到看板

---

## 二、上手指南

> 以下命令均可直接复制执行，从零到跑通 Demo 约 5 分钟。

### 2.1 环境准备

**必需：**

| 依赖 | 版本要求 | 安装方式 |
|------|---------|---------|
| Python | 3.8+ | [python.org](https://python.org) 下载安装 |
| lark-cli | 最新版 | 飞书开放平台工具 |

**验证环境：**

```bash
python --version     # 应输出 3.8+
lark-cli --version    # 应输出版本号
```

### 2.2 安装与认证

**Step 1：下载项目**

```bash
# 如果有 git
git clone <项目仓库地址>
cd SkillGuard

# 或者直接下载 zip 解压后进入目录
cd SkillGuard
```

**Step 2：lark-cli 认证（关键步骤）**

```bash
# 登录飞书账号（会打开浏览器授权）
lark-cli auth login

# 验证认证状态
lark-cli auth status
```

> 认证成功后会显示当前登录的飞书用户信息。如遇权限不足，执行 `lark-cli auth login --domain all` 重新授权。

**Step 3：验证多维表格可访问**

```bash
# 列出当前 Base 的表结构（确认权限正常）
lark-cli base +tables-list --base-token YOUR_BASE_TOKEN --as user --format json
```

应返回 3 张表（Issues / Versions / Forks）的元数据。

### 2.3 配置（一键完成）

项目已预置 `config.json`，包含 Base token、表 ID、视图 ID、表单 ID，**无需手动修改**：

```json
{
    "base_token": "YOUR_BASE_TOKEN",
    "base_url": "https://your-domain.feishu.cn/base/YOUR_BASE_TOKEN",
    "tables": { "issues": "...", "versions": "...", "forks": "..." },
    "views": { "issues_kanban": "..." },
    "forms": { "issues_feedback": "..." }
}
```

**初始化看板视图和表单（只需运行一次）：**

```bash
python setup_views.py
```

预期输出：

```
✓ 看板视图已就绪
✓ 表单问题已验证
  ✓ 表单状态正确：8 个用户问题，无 AI/系统字段泄露
```

### 2.4 运行 Demo

**一键运行完整 5 步流程：**

```bash
python demo.py
```

脚本会自动执行：

| 步骤 | 动作 | 预期输出 |
|------|------|---------|
| ① 插入 Issue | 向 Issues 表插入 3 个真实场景问题 | `✓ 已插入 3 个示例 Issue` |
| ② AI 分流 | 复现 → Agent 对比 → 根因 → 判定 | `✓ AI 分流完成：确认Bug 1 / 非Bug 1 / 待确认 1` |
| ③ 版本记录 | 为确认 Bug 创建修复版本 | `✓ 已创建版本记录 v1.2.1-fix` |
| ④ Fork 演示 | 创建 Fork + 上游 Diff | `✓ Fork 记录已创建` |
| ⑤ IM 通知 | 发送 Interactive Card | `✓ IM 卡片已发送` |

**运行 Fork 合并演示：**

```bash
python fork_merge.py
```

### 2.5 在线查看数据

打开飞书多维表格，即可看到完整 Demo 数据：

> **Base 链接**：https://your-domain.feishu.cn/base/YOUR_BASE_TOKEN

| 表 | 内容 | 重点看 |
|----|------|--------|
| Issues | 3 个示例 Issue | AI 判定列、置信度、根因分析 |
| Versions | 1 条修复版本 | CHANGELOG、关联 Issue |
| Forks | 1 条 Fork 记录 | Diff 摘要、合并状态 |

切换到 **看板视图**可按状态分组查看 Issue 流转。

### 2.6 演示文稿

浏览器打开 `skillguard-presentation.html`，10 页深色主题幻灯片：
- 第 5-7 页含**可交互 UI 展示**（点击按钮体验 IM 卡片三态流转、开发者操作卡片、Issue 看板）
- 左右键 / 滚轮翻页

### 2.7 常见问题

| 问题 | 解决方案 |
|------|---------|
| `lark-cli: command not found` | 未安装 lark-cli，参考飞书开放平台文档安装 |
| `auth: not logged in` | 执行 `lark-cli auth login` 重新登录 |
| `base: permission denied` | 确认飞书账号有该 Base 的访问权限，或执行 `lark-cli auth login --domain all` |
| `record-batch-create: parse failed` | 检查 config.json 中的 token 和 table_id 是否正确 |
| IM 卡片未收到 | 确认 lark-cli 有 IM 发送权限，且目标用户在应用可见范围内 |
| PowerShell JSON 报错 | 已通过文件引用方式处理，如仍报错请用 cmd 或 Git Bash 运行 |

---

## 三、Demo 录制方案

### 3.1 视频规格

| 项 | 要求 |
|----|------|
| 时长 | 不超过 3 分钟（180 秒） |
| 形式 | 实机演示，真人旁白或字幕 |
| 画幅 | 1920×1080（横屏）|
| 格式 | MP4 |

### 3.2 各步骤技术实现说明

> 录制前必读：理解幕后逻辑，旁白和应对评委提问时心里有底。

**核心要点：一条 `python demo.py` 命令串起 5 个步骤，脚本自动顺序执行，无需手动干预。**

| 步骤 | 终端输出标题 | 幕后做了什么 | 调用的飞书 API |
|------|-------------|-------------|---------------|
| ① 插入 Issue | `📋 步骤 1: 插入示例 Issue` | Python 构造 3 条真实场景数据（附件发送失败 / 文档解析超时 / 日历重复创建），写入临时 JSON 文件，通过 lark-cli 调飞书多维表格 API 创建记录 | `base +record-batch-create` |
| ② AI 诊断 | `🔍 步骤 2: AI 智能分流` | Python 读取 Issues 表所有"待分流"记录 → 对每条执行模拟 AI 四步法（复现测试 → 多 Agent 对比 → 根因定位 → 判定）→ 将判定结果、置信度、根因分析、修复分支名写回 Issues 表 | `base +record-list` → `base +record-batch-update` |
| ③ 版本记录 | `📦 步骤 3: 为确认 Bug 创建版本记录` | Python 查找所有判定为"确认Bug"的 Issue → 为每条创建修复版本记录（版本号 v1.2.1-fix + CHANGELOG + 关联 Issue ID），写入 Versions 表 | `base +record-list` → `base +record-batch-create` |
| ④ Fork 演示 | `🔄 步骤 4: Fork & 上游 Diff 演示` | Python 构造一条 Fork 记录（Fork 名称、原版版本、自定义修改说明、上游版本、Diff 摘要、合并状态），写入 Forks 表 | `base +record-batch-create` |
| ⑤ IM 通知 | `📨 步骤 5: IM 卡片通知开发者` | Python 聚合所有已诊断 Issue → 构建 Interactive Card 2.0 JSON（含归因统计 + 每条 Issue 详情 + 确认 Bug / 标记误报 / 转人工按钮）→ 通过 lark-cli 发送到开发者飞书 IM | `base +record-list` → `im +messages-send --msg-type interactive` |
| ⑥ 卡片回调 | `🎮 步骤 6: 模拟卡片交互回调` | 模拟开发者点击「确认 Bug」按钮 → Python 找到对应 Issue → 更新状态为"已确认"（看板同步刷新）→ 打印实际部署时的回调流程说明 | `base +record-list` → `base +record-batch-update` |
| 附：Fork 合并 | `🔄 SkillHub+ Fork & Merge Demo` | **独立脚本** `python fork_merge.py` — 读取待合并 Fork → 逐条展示 Diff（文件名 / 行号 / 变更内容 / 安全or冲突标记）→ 模拟合并决策 → 更新 Forks 表合并状态 | `base +record-list` → `base +record-batch-update` |

> `demo.py` = 6 步端到端，一条命令跑完；`fork_merge.py` = 独立的 Fork 合并演示，单独运行。两者共享同一个 config.json 和飞书 Base。

### 3.3 录制脚本（分镜表）

> 原则：**先展示产品能力和效果（0:00-1:00），再实机跑一遍核心流程（1:00-2:30），最后展示 IM 交互（2:30-2:50）**

**录制前准备：**
1. 终端字体调到 18px+，确保观众能看清输出
2. 浏览器打开飞书 Base（看板视图），窗口可快速切换
3. 飞书客户端打开，确保能收到 IM 消息
4. 建议分段录制后剪辑拼接：产品展示段 → Demo 运行段 → IM 交互段

#### 第一段：产品展示（0:00 - 1:00）

| 时间 | 画面 | 旁白/字幕 | 操作 |
|------|------|----------|------|
| **0:00-0:08** | 黑屏打字动画 | "同一个 Skill，不同 Agent 调用，效果天差地别" | 标题动画 |
| **0:08-0:15** | 演示文稿封面 | "SkillHub+ — AI Skill 兼容性诊断平台" | 浏览器打开 `skillguard-presentation.html` |
| **0:15-0:25** | 演示文稿第 2-3 页 | "用户无处反馈，开发者无从知晓。SkillHub+ 构建兼容性诊断闭环" | 按 → 翻页，展示痛点 + 架构图 |
| **0:25-0:35** | 演示文稿第 4 页 | "AI 四步法：问题复现 → 多环境横测 → 适配定位 → 归因判定" | 按 → 翻页 |
| **0:35-0:50** | 飞书多维表格看板 | "Issues 表自动收集用户反馈，AI 已完成诊断，看板视图一目了然" | 切到浏览器 Base，切到看板视图，展示状态分组 |
| **0:50-1:00** | Issues 表详情 | "每个 Issue 都有归因分类、适配建议、兼容性矩阵" | 切到表格视图，滚动展示 AI 判定、置信度、根因分析字段 |

#### 第二段：实机演示（1:00 - 2:30）

> 核心要点：终端输入 `python demo.py` 回车后，5 步自动顺序执行。录制时在每步输出后停顿，切到飞书 Base 展示对应结果。

| 时间 | 画面 | 旁白/字幕 | 操作 | 幕后 |
|------|------|----------|------|------|
| **1:00-1:05** | 终端 | "接下来，一条命令跑完完整流程" | 输入 `python demo.py` 回车 | 脚本启动，读取 config.json |
| **1:05-1:15** | 终端 · 步骤 1 | "Step 1：向 Issues 表插入 3 个真实场景的问题" | 展示 `✓ 附件发送失败` `✓ 文档解析超时` `✓ 日历事件重复创建` | lark-cli 调飞书 API 写入 3 条记录 |
| **1:15-1:30** | 终端 · 步骤 2 | "Step 2：AI 兼容性诊断——复现 → 多环境横测 → 适配定位 → 归因判定" | 展示动画输出 `复现测试中... ✓ → 多环境横测中... ✓ → 适配定位中... ✓` | Python 模拟四步法，lark-cli 写回判定结果 |
| **1:30-1:38** | 飞书 Base | "回到表格，Issues 的 AI 字段已自动填充" | 切到浏览器 Base，展示 AI 判定、置信度、根因分析列 | 步骤 2 的写入结果 |
| **1:38-1:50** | 终端 · 步骤 3 | "Step 3：为 Skill Bug 自动创建修复版本记录" | 展示 `✓ lark-mail v1.2.1-fix (修复 ISSUE-xxxx)` | lark-cli 向 Versions 表创建修复版本 |
| **1:50-1:58** | 飞书 Base | "Versions 表已自动生成修复版本" | 切到浏览器 Base，展示 Versions 表 | 步骤 3 的写入结果 |
| **1:58-2:10** | 终端 · 步骤 4 | "Step 4：创建 Fork 记录，展示上游 Diff" | 展示 `✓ Fork 创建: lark-mail-custom` 和 Diff 变更摘要 | lark-cli 向 Forks 表创建记录 |
| **2:10-2:18** | 飞书 Base | "Forks 表记录了 Fork 版本、上游变更、合并状态" | 切到浏览器 Base，展示 Forks 表 | 步骤 4 的写入结果 |
| **2:18-2:30** | 终端 · 步骤 5 | "Step 5：将诊断报告以交互卡片发送到开发者飞书" | 展示 `✓ Interactive Card 已发送到飞书（含操作按钮）` | lark-cli 构建 Interactive Card 发送到飞书 IM |

#### 第三段：IM 交互展示（2:30 - 2:55）

| 时间 | 画面 | 旁白/字幕 | 操作 |
|------|------|----------|------|
| **2:30-2:40** | 飞书 IM | "开发者收到交互卡片，含归因统计和每个 Issue 的详情" | 切到飞书客户端，展示收到的 Interactive Card |
| **2:40-2:50** | 终端 · 步骤 6 | "Step 6：模拟开发者点击「确认 Bug」按钮，状态实时同步到看板" | 切回终端，展示回调模拟输出：状态更新为"已确认" |
| **2:50-2:55** | 飞书 Base | "看板视图自动刷新，Issue 已从确认Bug 流转到已确认" | 切到 Base 看板视图，展示状态变化 |

#### 第四段：Fork 合并演示 + 结尾（2:55 - 3:00）

| 时间 | 画面 | 旁白/字幕 | 操作 |
|------|------|----------|------|
| **2:55-3:00** | 演示文稿结尾页 | "让每个 Skill 都有反馈窗口，每个兼容性问题都能被快速定位和适配" | 切到演示文稿最后一页 |

> 如果时间紧张，第四段 Fork 合并可省略，直接切到结尾页。

### 3.4 录制技巧

1. **分段录制后拼接** — 产品展示段（0:00-1:00）→ Demo 运行段（1:00-2:30）→ IM 交互段（2:30-2:50），剪辑拼接，避免一镜到底的失误风险
2. **终端字体放大** — 录制前把终端字体调到 18px+，确保观众能看清输出
3. **飞书窗口单独录** — IM 卡片交互部分单独录制，确保画面清晰
4. **加字幕** — 用剪映或 PR 加字幕，确保无声音也能看懂
5. **节奏紧凑** — 每个画面不超过 15 秒，保持观众注意力

### 3.5 社媒发布

**发布平台（任选 1-2 个）：**

| 平台 | 话题标签 | 内容侧重 |
|------|---------|---------|
| 小红书 | #飞书AI绝活大会 #飞书开发 #AI编程 | 图文+视频，侧重产品价值和使用场景 |
| 抖音 | #飞书AI绝活大会 #飞书开发 #AI编程 | 短视频，前 3 秒抓住眼球 |
| B 站 | #飞书AI绝活大会 #飞书开发 #AI编程 | 投稿到科技区，标题带"飞书 Skill 质量管理" |
| 视频号 | #飞书AI绝活大会 #飞书开发 #AI编程 | 朋友圈传播，附项目链接 |

**发布文案模板：**

```
🔥 同一个 Skill，不同 Agent 调用效果天差地别？
用户无处反馈，开发者无从知晓？

SkillHub+ 来了！基于飞书多维表格 + lark-cli + AI 引擎，
为飞书 Skill 提供类 GitHub 的 Issue 反馈、AI 兼容性诊断、
版本管理和 Fork 合并能力。

✅ 用户表单反馈，零门槛提交
✅ AI 多环境横测 + 适配定位 + 归因判定
✅ 交互卡片推送，开发者卡片内直接操作
✅ Fork 上游 Diff，安全合并

#飞书AI绝活大会 #飞书开发 #AI编程
```

> 认真推销自己的产品！社媒表现会用于评选"社媒最受欢迎奖"。

---

## 四、文件清单

| 文件 | 作用 | 提交时附带 |
|------|------|-----------|
| `demo.py` | 主 Demo 脚本（5 步端到端） | ✅ |
| `setup_views.py` | 看板视图和表单配置 | ✅ |
| `fork_merge.py` | Fork 合并演示 | ✅ |
| `config.json` | Base 配置 | ✅ |
| `issues_fields.json` | Issues 表字段定义 | ✅ |
| `versions_fields.json` | Versions 表字段定义 | ✅ |
| `forks_fields.json` | Forks 表字段定义 | ✅ |
| `README.md` | 项目文档 | ✅ |
| `SUBMISSION.md` | 本提交材料 | ✅ |
| `skillguard-presentation.html` | 比赛演示文稿（10 页） | ✅ |
| `skillguard-test-report/` | 测试报告 | ✅ |
